<h1>Tasks — <%= current_household.name %></h1>

<%= link_to "New task", new_task_path, class: "btn btn-primary" %>

<div class="task-list">
  <% @tasks.each do |task| %>
    <%= render "tasks/task_card", task: task %>
  <% end %>
</div>