# Secrets for config/deploy.yml. Pull them from a password manager or your
# shell environment; never commit raw values. See `kamal secrets --help`.

# Registry token, e.g. a GitHub token with write:packages for ghcr.io.
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# Optional SMTP settings (enable the `secret:` list in config/deploy.yml).
# SMTP_HOST=$SMTP_HOST
# SMTP_USER=$SMTP_USER
# SMTP_PASSWORD=$SMTP_PASSWORD
